import { createAction } from "@ngrx/store";

export const loadAllProducts = createAction('[Products] Load');

export const allProductsLoaded = createAction('[Products Effect] Load');

export const addProduct = createAction('[ProductsEntryPanel] Load');

